import "./style.css";
import React from 'react';

const Navbar = () => {
    return (
        <div className="navbar-container">
            <a href="/">Logout</a>
        </div>
    );
};

export default Navbar;